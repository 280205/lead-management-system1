{
  "name": "lead-management-backend",
  "version": "1.0.0",
  "description": "Backend API for Lead Management System",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "npx prisma generate",
    "postbuild": "npx prisma migrate deploy",
    "migrate": "npx prisma migrate deploy", 
    "generate": "npx prisma generate",
    "seed": "node src/scripts/seed.js",
    "deploy": "npm run generate && npm run migrate && npm run seed"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cookie-parser": "^1.4.6",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.8.1",
    "@prisma/client": "^5.2.0",
    "prisma": "^5.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
